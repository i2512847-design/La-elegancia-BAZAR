[project]
name = "bazar"
version = "0.1.0"
description = "Sistema web de gestión de productos para un bazar usando Streamlit y Supabase"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "streamlit>=1.30.0",
    "supabase>=2.0.0",
    "python-dotenv>=1.0.1",
]
